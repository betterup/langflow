# Encrypted secrets for dev environment
# These would be encrypted with SOPS or stored in AWS Secrets Manager
# Placeholder for now - actual implementation would use external-secrets-operator

apiVersion: v1
kind: Secret
metadata:
  name: langflow-database-credentials
type: Opaque
stringData:
  DATABASE_PASSWORD: "placeholder-will-be-from-aws-secrets-manager"